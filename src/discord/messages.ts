import { t } from "@lingui/core/macro";

export const discordMessages = {
  unexpectedError: () =>
    t({
      id: "discord.common.unexpectedError",
      message: "Something went wrong. Try again later."
    })
};
